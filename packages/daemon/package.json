{
  "name": "@flotilla/daemon",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Flotilla local daemon CLI — pairs, receives agent runs, spawns runtime adapters.",
  "bin": {
    "flotilla-daemon": "./src/index.js"
  },
  "main": "./src/index.js",
  "scripts": {
    "start": "node src/index.js start",
    "test": "echo \"daemon tests run against the API realtime suite\" && exit 0"
  },
  "dependencies": {
    "@flotilla/shared": "*",
    "socket.io-client": "^4.8.1"
  }
}
